import { Component } from '@angular/core';

/** Port of the `#landing-page` static markup from legacy/index.html. */
@Component({
  selector: 'app-landing',
  imports: [],
  templateUrl: './landing.html',
  styleUrl: './landing.css',
})
export class Landing {}
